repositories {
    mavenCentral()
    gradlePluginPortal() // because we depends on gradle plugin for compilation
}

dependencies {
    implementation 'com.github.node-gradle:gradle-node-plugin:3.0.1'
}
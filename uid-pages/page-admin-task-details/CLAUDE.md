# Bonita Admin Task Details

This page provides the detail information of a task. It is dedicated to Admin-like profiles. This page can only be used as an orphan page as it needs an 'id' parameter to display some content.

- **Family:** UID page (community) — `community/uid-pages/page-admin-task-details`
- **Page id:** `custompage_adminTaskDetailsBonita` · **contentType:** page
- Generated by the UI Designer: **don't hand-edit `src/*.json`** — edit via `./gradlew runUID`.

## API endpoints

Declared in `src/resources/page.properties` (missing entry = `403` in production):

| Method | Endpoint |
|---|---|
| GET / POST | `bpm/comment` |
| GET | `bpm/archivedComment` |
| PUT | `bpm/humanTask` |
| PUT | `bpm/activity` |
| PUT | `bpm/activityReplay` |
| POST | `bpm/userTask` |
| GET / PUT | `bpm/flowNode` |
| GET | `bpm/archivedFlowNode` |
| GET | `bpm/connectorFailure` |
| GET | `identity/user` |
| GET | `system/session` |
| GET | `system/feature` |
| GET | `form/mapping` |
| GET | `bpm/connectorInstance` |
| GET | `bpm/archivedConnectorInstance` |
| GET | `bpm/userTask/*/contract` |
| GET | `bpm/userTask/*/context` |
| POST | `bpm/userTask/*/execution` |
| GET | `bpm/caseVariable` |
| GET | `bpm/activityVariable` |
| GET | `API/formsDocumentImage` |
| GET | `API/documentDownload` |
| GET | `bdm/businessData` |
| GET | `bdm/businessDataReference` |
| POST | `API/formFileUpload` |

## Conventions (auto-loaded rules)

- Artifact versioning (filename `…BonitaV<n>`, bump dev+1, cascade dependents) → `.claude/rules/uid/versioning.md`
- UID conventions (null init values, factorized step definitions, reusable fixtures, i18n workflow) → `.claude/rules/uid/conventions.md`

## Tests

Cucumber BDD: `test/specs/*.feature` + `step_definitions/`, mocks in `test/mockServer/json/`.
Run: `./gradlew :uid-pages:page-admin-task-details:runTestChrome` (shared config in `community/uid-pages/cypress.config.js`).

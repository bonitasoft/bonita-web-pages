# Bonita layout

This is the Bonita living application layout, offered for any new application created. Its header contains a logo placeholder, the name of the application, the logged user's details (with a language picker and a logout button), and an access to all applications of the logged user. The application content is rendered as an horizontal menu and an iframe for application pages. This layout requires font-awesome in order to be able to display the default user icons. This dependence should be available in the theme of the application where this layout will be used.

- **Family:** UID page (community) — `community/uid-pages/layout-bonita`
- **Page id:** `custompage_layoutBonita` · **contentType:** layout
- Generated by the UI Designer: **don't hand-edit `src/*.json`** — edit via `./gradlew runUID`.

## API endpoints

Declared in `src/resources/page.properties` (missing entry = `403` in production):

| Method | Endpoint |
|---|---|
| GET | `system/session` |
| GET | `system/i18nlocale` |
| GET | `identity/user` |
| GET | `living/application` |
| GET | `living/application-page` |
| GET | `living/application-menu` |
| GET | `API/avatars` |

## Conventions (auto-loaded rules)

- Artifact versioning (filename `…BonitaV<n>`, bump dev+1, cascade dependents) → `.claude/rules/uid/versioning.md`
- UID conventions (null init values, factorized step definitions, reusable fixtures, i18n workflow) → `.claude/rules/uid/conventions.md`

## Tests

Cucumber BDD: `test/specs/*.feature` + `step_definitions/`, mocks in `test/mockServer/json/`.
Run: `./gradlew :uid-pages:layout-bonita:runTestChrome` (shared config in `community/uid-pages/cypress.config.js`).

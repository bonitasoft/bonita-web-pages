# Bonita Admin Install Export Organization

This page is dedicated to Administrator-like profiles. It allows to install or export the .xml file of an organization.

- **Family:** UID page (community) — `community/uid-pages/page-admin-install-export-organization`
- **Page id:** `custompage_adminInstallExportOrganizationBonita` · **contentType:** page
- Generated by the UI Designer: **don't hand-edit `src/*.json`** — edit via `./gradlew runUID`.

## API endpoints

Declared in `src/resources/page.properties` (missing entry = `403` in production):

| Method | Endpoint |
|---|---|
| GET | `system/session` |
| GET | `API/exportOrganization` |
| POST | `API/formFileUpload` |
| POST | `organization/import` |

## Conventions (auto-loaded rules)

- Artifact versioning (filename `…BonitaV<n>`, bump dev+1, cascade dependents) → `.claude/rules/uid/versioning.md`
- UID conventions (null init values, factorized step definitions, reusable fixtures, i18n workflow) → `.claude/rules/uid/conventions.md`

## Tests

Cucumber BDD: `test/specs/*.feature` + `step_definitions/`, mocks in `test/mockServer/json/`.
Run: `./gradlew :uid-pages:page-admin-install-export-organization:runTestChrome` (shared config in `community/uid-pages/cypress.config.js`).

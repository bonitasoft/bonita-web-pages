# Bonita Admin Case Details

This page provides the detail information of a case. It is dedicated to Admin-like profiles. This page can only be used as an orphan page as it needs an 'id' parameter to display some content.

- **Family:** UID page (community) — `community/uid-pages/page-admin-case-details`
- **Page id:** `custompage_adminCaseDetailsBonita` · **contentType:** page
- Generated by the UI Designer: **don't hand-edit `src/*.json`** — edit via `./gradlew runUID`.

## API endpoints

Declared in `src/resources/page.properties` (missing entry = `403` in production):

| Method | Endpoint |
|---|---|
| GET | `bpm/case` |
| GET | `bpm/archivedCase` |
| GET | `bpm/diagram` |
| GET | `bpm/process` |
| GET | `bpm/case/*/context` |
| GET | `bpm/archivedCase/*/context` |
| GET / PUT | `bpm/caseVariable` |
| GET | `bpm/archivedCaseVariable` |
| GET | `bpm/flowNode` |
| GET | `bpm/task` |
| GET | `bpm/archivedFlowNode` |
| GET | `bpm/archivedTask` |
| GET | `bpm/humanTask` |
| GET | `bpm/archivedHumanTask` |
| GET | `bpm/caseDocument` |
| GET | `bpm/document` |
| GET | `bpm/archiveddocument` |
| GET | `bpm/archivedCaseDocument` |
| GET / POST | `bpm/comment` |
| GET | `bpm/archivedComment` |
| GET | `API/documentDownload` |
| GET | `portal/documentDownload` |
| GET | `portal/formsDocumentDownload` |
| GET | `portal/downloadDocument` |
| GET | `system/session` |
| GET | `bdm/businessData` |
| GET | `bdm/businessDataReference` |

## Conventions (auto-loaded rules)

- Artifact versioning (filename `…BonitaV<n>`, bump dev+1, cascade dependents) → `.claude/rules/uid/versioning.md`
- UID conventions (null init values, factorized step definitions, reusable fixtures, i18n workflow) → `.claude/rules/uid/conventions.md`

## Tests

Cucumber BDD: `test/specs/*.feature` + `step_definitions/`, mocks in `test/mockServer/json/`.
Run: `./gradlew :uid-pages:page-admin-case-details:runTestChrome` (shared config in `community/uid-pages/cypress.config.js`).
